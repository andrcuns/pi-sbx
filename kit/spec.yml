schemaVersion: "1"
kind: agent
name: pi
agent:
  image: "herdr-sbx:latest"
  entrypoint:
    run: [herdr]

commands:
  install:
    - command: "npm install -g --ignore-scripts @earendil-works/pi-coding-agent"
      user: "1000"
      description: "Install pi coding harness"
