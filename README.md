# pi-sbx

Custom docker sandbox template for [pi](https://pi.dev/) harness with [mise](https://mise.run) preinstalled.

## Usage

```shell
sbx run pi --kit "git+https://github.com/andrcuns/pi-sbx.git#ref=main&dir=kit"
```
